---
"@nxsflow/amplify-email": minor
---

Initial release: defineEmail() CDK construct with SES + DNS + Lambda, standard email template, output registration, and server-side generateClient() singleton.
